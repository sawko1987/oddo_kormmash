# Исправление проблемы инициализации базы данных

## ✅ Что исправлено

Проблема совместимости с Python 3.14 исправлена:
- Добавлен метод `copy()` в класс `OrderedSet` в файле `odoo/tools/misc.py`
- Это решает ошибку: `AttributeError: 'OrderedSet' object has no attribute 'copy'`

## 🚀 Запуск инициализации базы данных

Теперь вы можете запустить инициализацию базы данных. Выполните команду в терминале:

### Вариант 1: Через PowerShell или CMD (рекомендуется для Windows)

```powershell
cd C:\oddo_kormmash
python odoo-bin -c odoo.conf -d odoo --init=base --stop-after-init
```

### Вариант 2: Через Git Bash

```bash
cd /c/oddo_kormmash
python odoo-bin -c odoo.conf -d odoo --init=base --stop-after-init
```

## 📋 Что должно произойти

При успешной инициализации вы увидите:
- Создание таблиц базы данных
- Загрузка модуля `base`
- Установка базовых данных
- Сообщение об успешном завершении

Процесс может занять несколько минут.

## ⚠️ Если возникают проблемы

### Проверьте PostgreSQL
```bash
docker ps | grep postgres
```

Должен быть запущен контейнер `odoo_postgres`.

### Проверьте конфигурацию
Убедитесь, что в `odoo.conf` указан правильный пароль:
```
db_password = odoo_password
```

### Проверьте логи
```bash
tail -f var/odoo.log
```

## 🎯 После успешной инициализации

Запустите Odoo сервер:

```bash
python odoo-bin -c odoo.conf
```

Затем откройте в браузере: **http://localhost:8069**

## 📝 Примечание

Если команда не выполняется в Git Bash, попробуйте использовать:
- PowerShell
- CMD (командная строка Windows)
- Или запустите через Python напрямую:

```python
python -m odoo.cli.server -c odoo.conf -d odoo --init=base --stop-after-init
```
