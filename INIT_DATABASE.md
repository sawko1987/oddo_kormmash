# Инструкция по инициализации базы данных Odoo

## ✅ Исправления выполнены

1. **Исправлена ошибка совместимости с Python 3.14**
   - Добавлен метод `copy()` в класс `OrderedSet`
   - Файл: `odoo/tools/misc.py`

2. **Установлен недостающий модуль**
   - `pyOpenSSL` установлен

## 🚀 Запуск инициализации

Выполните следующую команду в терминале (PowerShell, CMD или Git Bash):

```bash
python odoo-bin -c odoo.conf -d odoo --init=base --stop-after-init
```

### Что происходит при инициализации:

1. Подключение к базе данных PostgreSQL
2. Создание структуры базы данных (таблицы)
3. Загрузка модуля `base`
4. Установка базовых данных
5. Завершение и остановка сервера

**Время выполнения:** 2-5 минут

## 📋 Ожидаемый вывод

При успешной инициализации вы увидите:
```
Loading module base...
Installing base...
...
Database initialized successfully
```

## ⚠️ Если возникают ошибки

### Ошибка подключения к БД
Проверьте, что PostgreSQL запущен:
```bash
docker ps | grep postgres
```

Если не запущен:
```bash
docker-compose up -d
```

### Ошибка "ModuleNotFoundError"
Установите недостающий модуль:
```bash
python -m pip install <имя_модуля>
```

### Проверка логов
```bash
tail -f var/odoo.log
```

## 🎯 После успешной инициализации

Запустите Odoo сервер:

```bash
python odoo-bin -c odoo.conf
```

Затем откройте в браузере: **http://localhost:8069**

При первом входе вы увидите мастер настройки Odoo.

## 📝 Альтернативный способ запуска

Если команда не работает в Git Bash, используйте PowerShell:

```powershell
cd C:\oddo_kormmash
python odoo-bin -c odoo.conf -d odoo --init=base --stop-after-init
```

Или через Python модуль:

```bash
python -m odoo.cli.server -c odoo.conf -d odoo --init=base --stop-after-init
```
