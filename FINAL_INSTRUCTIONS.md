# Финальные инструкции по запуску Odoo

## ✅ Что уже сделано

1. ✅ Все Python зависимости установлены
2. ✅ PostgreSQL запущен через Docker
3. ✅ Исправлена ошибка совместимости с Python 3.14 (OrderedSet.copy())
4. ✅ Установлены недостающие модули (pyOpenSSL, PyPDF2)

## 🚀 Запуск Odoo

### Способ 1: Через веб-интерфейс (САМЫЙ ПРОСТОЙ)

1. **Запустите Odoo сервер:**
   ```bash
   python odoo-bin -c odoo.conf
   ```

2. **Откройте браузер:** http://localhost:8069

3. **В веб-интерфейсе:**
   - Вы увидите страницу создания базы данных
   - Введите имя базы: `odoo`
   - Выберите язык
   - Выберите страну
   - Нажмите "Create database"
   - Дождитесь завершения инициализации (2-5 минут)

4. **После инициализации** откроется мастер настройки Odoo

### Способ 2: Через командную строку

**В PowerShell или CMD:**

```powershell
# Инициализация базы данных
python odoo-bin -c odoo.conf -d odoo --init=base --stop-after-init --without-demo=all

# Запуск сервера
python odoo-bin -c odoo.conf
```

**В Git Bash:**

```bash
# Инициализация базы данных
python odoo-bin -c odoo.conf -d odoo --init=base --stop-after-init --without-demo=all

# Запуск сервера
python odoo-bin -c odoo.conf
```

### Способ 3: Использовать готовый скрипт (Windows)

Запустите файл `start_odoo.bat` двойным кликом или через командную строку.

## ⚠️ Если возникают проблемы

### Проблема: "OrderedSet object has no attribute 'copy'"

**Решение:** Исправление уже применено. Если ошибка повторяется:
1. Убедитесь, что используете исправленный файл `odoo/tools/misc.py`
2. Перезапустите Python (закройте все процессы python.exe)

### Проблема: "ModuleNotFoundError"

**Решение:** Установите недостающий модуль:
```bash
python -m pip install <имя_модуля>
```

### Проблема: "relation does not exist"

**Решение:** База данных не инициализирована. Используйте веб-интерфейс (Способ 1) или пересоздайте базу:
```bash
docker exec odoo_postgres psql -U sawko1987 -d postgres -c "DROP DATABASE odoo;"
docker exec odoo_postgres psql -U sawko1987 -d postgres -c "CREATE DATABASE odoo OWNER sawko1987;"
```

### Проблема: PostgreSQL не запущен

**Решение:**
```bash
docker-compose up -d
```

## 📋 Проверка статуса

### Проверить PostgreSQL:
```bash
docker ps | grep postgres
```

### Проверить логи Odoo:
```bash
tail -f var/odoo.log
```

### Проверить процессы Python:
```powershell
tasklist | findstr python
```

## 🎯 Рекомендации

1. **Используйте веб-интерфейс** для первого запуска - это самый надежный способ
2. **Для стабильной работы** рассмотрите использование Python 3.12 вместо 3.14
3. **После успешного запуска** Odoo будет доступен по адресу: http://localhost:8069

## 📝 Конфигурация

- **База данных:** odoo
- **Пользователь БД:** sawko1987
- **Пароль БД:** odoo_password
- **Порт HTTP:** 8069
- **Порт PostgreSQL:** 5432

## ✅ Готово к запуску!

Все необходимое установлено и настроено. Запустите Odoo любым из способов выше!
