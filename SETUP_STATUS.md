# Статус установки Odoo

## ✅ Выполнено

1. **Виртуальное окружение Python** - создано в `venv/`
2. **Python зависимости** - установлены из `requirements.txt`
   - Использован `psycopg2-binary` вместо `psycopg2` (не требует компиляции)
   - `python-ldap` пропущен (требует системных библиотек, опционален)
3. **Odoo пакет** - установлен в режиме разработки (`pip install -e .`)
4. **Конфигурационный файл** - создан `odoo.conf` с настройками по умолчанию
5. **Директория для данных** - создана `var/` для логов и данных

## ⚠️ Требуется выполнить вручную (нужны права sudo)

### 1. Установка PostgreSQL

```bash
sudo apt-get update
sudo apt-get install -y postgresql postgresql-contrib
```

### 2. Установка системных зависимостей

```bash
sudo apt-get install -y \
    python3-dev \
    libpq-dev \
    libxml2-dev \
    libxslt1-dev \
    libjpeg-dev \
    libpng-dev \
    libfreetype6-dev \
    liblcms2-dev \
    libwebp-dev \
    libharfbuzz-dev \
    libfribidi-dev \
    libxcb1-dev \
    zlib1g-dev \
    libsass-dev \
    node-less \
    build-essential
```

### 3. Настройка базы данных

```bash
sudo -u postgres createuser -s odoo
sudo -u postgres createdb odoo
```

Или установить пароль для пользователя:
```bash
sudo -u postgres psql -c "ALTER USER odoo WITH PASSWORD 'your_password';"
```

Затем обновить `odoo.conf` с паролем, если установлен.

## 🚀 После установки PostgreSQL

### Инициализация базы данных (первый запуск)

```bash
cd "/home/sawko1987/ Odoo"
source venv/bin/activate
python3 odoo-bin -d odoo --init=base --stop-after-init
```

### Запуск сервера

```bash
python3 odoo-bin -c odoo.conf
```

Или в режиме разработки:
```bash
python3 odoo-bin -d odoo --dev=all
```

Сервер будет доступен по адресу: **http://localhost:8069**

## 📝 Файлы

- `odoo.conf` - конфигурационный файл
- `INSTALL_INSTRUCTIONS.md` - подробные инструкции
- `check_setup.sh` - скрипт проверки готовности системы

## 🔍 Проверка готовности

Запустите скрипт проверки:
```bash
./check_setup.sh
```
