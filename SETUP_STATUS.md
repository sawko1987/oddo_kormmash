# Статус установки и настройки Odoo

## ✅ Выполнено

1. **Python зависимости установлены**
   - Все необходимые пакеты из `requirements.txt` установлены
   - psycopg2-binary установлен для работы с PostgreSQL на Windows
   - Odoo успешно импортируется и готов к запуску

2. **Конфигурация проверена**
   - Файл `odoo.conf` настроен
   - Пути к аддонам указаны правильно

3. **Docker конфигурация создана**
   - Файл `docker-compose.yml` готов для запуска PostgreSQL

## ❌ Требуется выполнить

### PostgreSQL не установлен/не запущен

Для запуска Odoo необходимо установить и запустить PostgreSQL. Есть два варианта:

#### Вариант 1: Запустить PostgreSQL через Docker (рекомендуется, если Docker Desktop установлен)

1. **Запустите Docker Desktop** (если не запущен)
   - Найдите "Docker Desktop" в меню Пуск и запустите
   - Дождитесь полного запуска (иконка в трее станет зеленой)

2. **Запустите PostgreSQL контейнер:**
   ```bash
   docker-compose up -d
   ```

3. **Обновите пароль в odoo.conf:**
   Откройте `odoo.conf` и установите:
   ```
   db_password = odoo_password
   ```

4. **Запустите Odoo:**
   ```bash
   source venv/bin/activate
   python odoo-bin -c odoo.conf
   ```

#### Вариант 2: Установить PostgreSQL напрямую

1. Скачайте и установите PostgreSQL с официального сайта:
   https://www.postgresql.org/download/windows/

2. Во время установки:
   - Запомните пароль для пользователя `postgres`
   - Порт оставьте 5432

3. Создайте пользователя и базу данных:
   ```sql
   -- Откройте pgAdmin или psql и выполните:
   CREATE USER sawko1987 WITH PASSWORD 'ваш_пароль';
   CREATE DATABASE odoo OWNER sawko1987;
   ```

4. Обновите `odoo.conf` с правильным паролем

5. Запустите Odoo:
   ```bash
   source venv/bin/activate
   python odoo-bin -c odoo.conf
   ```

## 📝 Команды для запуска

После установки PostgreSQL:

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Первый запуск (инициализация базы)
python odoo-bin -c odoo.conf -d odoo --init=base --stop-after-init

# Обычный запуск
python odoo-bin -c odoo.conf
```

## 🌐 Доступ к Odoo

После успешного запуска Odoo будет доступен по адресу:
- **http://localhost:8069**

## 📋 Текущая конфигурация

- **Пользователь БД:** sawko1987
- **База данных:** odoo
- **Порт PostgreSQL:** 5432
- **Порт HTTP:** 8069
- **Python:** 3.14.2
- **Odoo версия:** 19.0

## ⚠️ Важные замечания

1. Убедитесь, что пароль в `odoo.conf` соответствует паролю пользователя PostgreSQL
2. Если используете Docker, убедитесь, что Docker Desktop запущен
3. При первом запуске Odoo создаст веб-интерфейс для настройки базы данных
